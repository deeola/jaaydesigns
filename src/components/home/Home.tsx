import React, { FC } from "react";
import Header from "../shared/Header";

const Home: FC = () => {
  return (
    <div className="home">
      <Header />
      <div className="navLine"></div>
    </div>
  );
};

export default Home;
